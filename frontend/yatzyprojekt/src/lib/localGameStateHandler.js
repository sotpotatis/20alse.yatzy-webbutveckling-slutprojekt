/* localGameStateHandler.js
Det finns ett lokalt spelarläge. Här definierar vi de olika funktionerna för det. */
import { calculateAllPoints, possibleDiceStates } from "";
function onDoneButtonClick(gameState) {
    console.log("Klar-knappen har klickats på.")
}
function onReRollButtonClick(gameState) {
    
}
function onScoreClaim(gameState) {
    
}